<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.cbz.example.mapper.EmployeeMapper">

    <sql id="baseSqlId">
        Id,
        Name,
        Password,
        Email,
        PhoneNumber,
        DepartmentId,
        Type
    </sql>

    <sql id="camelSqlId">
        Id,
        Name,
        Password,
        Email,
        PhoneNumber,
        DepartmentId,
        Type
    </sql>
</mapper>
